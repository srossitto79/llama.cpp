set(TARGET llama-finetune-qlora)
add_executable(${TARGET} finetune_qlora.cpp critical-token-sft.cpp)
install(TARGETS ${TARGET} RUNTIME)
target_link_libraries(${TARGET} PRIVATE llama-common llama ${CMAKE_THREAD_LIBS_INIT})
target_compile_features(${TARGET} PRIVATE cxx_std_17)

if (BUILD_TESTING)
    add_executable(test-critical-token-sft test-critical-token-sft.cpp critical-token-sft.cpp)
    target_compile_features(test-critical-token-sft PRIVATE cxx_std_17)
    target_include_directories(test-critical-token-sft PRIVATE ${PROJECT_SOURCE_DIR}/vendor)
    target_compile_definitions(test-critical-token-sft PRIVATE CRITICAL_TOKEN_SFT_SOURCE_DIR="${CMAKE_CURRENT_SOURCE_DIR}")
    target_link_libraries(test-critical-token-sft PRIVATE ggml)
    add_test(NAME test-critical-token-sft COMMAND $<TARGET_FILE:test-critical-token-sft>)
endif()
